using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using ServerMiniCheckers.Utility;

namespace MiniCheckersUnitTests
{
    [TestClass]
    public class ServerDatabaseTests
    {
        [TestMethod]
        public void ValidLoginLogout()
        {
         //   var server = new DatabaseServer();
         //   Assert.IsTrue(server.IsValidLogin("jack", "12345"));
         //   Assert.IsTrue(server.IsValidLogout("jack"));
        }
    }
}
